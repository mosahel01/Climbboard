{
  "name": "bazarirank",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack real-time leaderboard — React + Express + Redis + Socket.IO",
  "scripts": {
    "redis": "npm --prefix backend run redis",
    "redis:stop": "npm --prefix backend run redis:stop",
    "seed": "npm --prefix backend run seed:reset",
    "dev": "npm --prefix backend run start & npm --prefix frontend run dev",
    "start": "npm --prefix backend run start",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test && npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18"
  }
}